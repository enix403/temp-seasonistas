SCOPE: General

GET => /api/me/profile
Get the current user's profile

PUT => /api/me/profile
Allows the user to update their profile (e.g., name, profile picture, bio, skills, location).

GET => /api/user/:userId
View a user

----------
SCOPE: Employer

POST => /api/job/post
Allows an employer to post a new job (job title, description, location, qualifications, etc.).

GET => /api/employer/my-postings
Returns a list of jobs posted by the employer, including active and inactive job postings.

----------
SCOPE: Employee

GET => /api/job/search
Allows the employee to search for jobs using filters like job title, location, job type (full-time/part-time), etc.

GET => /api/job/:jobId
Returns detailed information about a specific job posting.

POST => /api/job/:jobId/apply
Allows the employee to apply for a job by submitting required information (cover letter, resume, etc.).

GET => /api/job/application/:applicationId
View details of a specific application.

GET => /api/employee/my-applications
List of applications created by an employee

----------
SCOPE: Employer

GET => /api/job/:jobId/applicants
Fetches a list of employees who have applied for a specific job posting.

PUT => /api/job/:jobId/update-status
Mark a job posting as active or inactive.

----------

GET => /api/admin/users
Fetches a list of all users (employees and employers) for administrative purposes.

PUT => /api/admin/user/:userId/ban
Allows the admin to deactivate or ban a user from the platform.

=============























GET => /api/company/:companyId/employees
Fetches a list of employees who currently work or have worked for a specific employer.

GET => /api/job/post-template
Allows employers to use a previous job posting as a template for a new one (if available).

GET => /api/employer/:employerId/employees
Fetches a list of employees currently or previously working for the employer.

PUT => /api/admin/user/:userId/role
Allows the admin to update the role of a user (e.g., promoting an employee to an employer).
